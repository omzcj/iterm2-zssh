# iterm2-zssh

## Release

Update `VERSION` and push the change to `main`. The release workflow validates
both scripts and publishes `iterm2-zssh-VERSION.tar.gz` to a GitHub Release
tagged `vVERSION`.
